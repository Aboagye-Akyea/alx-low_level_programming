#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point A-Z a-z
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
	putchar(low);

	for (low = 'A'; low <= 'z'; low++)
	putcharlow();
	putchar('\n');

	return (0);
}
